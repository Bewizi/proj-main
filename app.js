const cardInfo = {
  cardName: "John Smith",
  cardNumber: `${123}-${456}-${789}-${468}-${378}`,
};

document.querySelector("form").addEventListener("submit", function (e) {
  e.preventDefault();
});
